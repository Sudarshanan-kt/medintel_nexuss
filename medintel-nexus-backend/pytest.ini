[pytest]
# Scopes the backend tests to this directory so they do not inherit the repository-root
# pytest.ini (which targets the automation suites). Keeps `from main import app` resolvable.
pythonpath = .
testpaths = tests
addopts = --tb=short -ra
